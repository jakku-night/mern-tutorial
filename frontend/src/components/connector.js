import React, { Children } from 'react';

const Connector = (Children) => {
    const [connection, setConnection] = useState({status: 'OK'});
    const conn = useContext('conn');
    const get_socket = async () => {
        try{
            const 
        }catch(error){
            
        }
    };

}; 